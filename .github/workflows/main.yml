on: [push, release]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Test
    steps:

      - name: Checkout
        uses: actions/checkout@v2

      - name: Test Action
        run: bash ./entrypoint_test.sh